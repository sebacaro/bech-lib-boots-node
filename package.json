{
  "name": "bech-estilos",
  "version": "1.1.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:package": "  ",
    "test": " ",
    "prepublish": "rm -Rf dist && mkdir dist && cp -r scss dist/scss && cp -r assets dist/assets && cp package.json dist",
    "publish": "npm publish dist"
  },
  "repository": {
    "type": "git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.1.3",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.4"
  }
}
